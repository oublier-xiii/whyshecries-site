// js/cursor.js — DISABLED
// The custom cursor was removed from the design (it caused visibility issues
// with the dark background and was invasive on mobile). This file is kept as
// a stub so the <script> tag in HTML doesn't 404, but it does nothing.
(() => {
  'use strict';
  // No-op
})();
